import styled from 'styled-components';

const UserInfoBody = styled.div`
  display: flex;
  margin-bottom: 15px;

  .userInfo__text {
    font-size: 12px;
    line-height: 16px;
    
    margin-left: 10px;

    color: var(--gray);
  }
`;

export default UserInfoBody;
